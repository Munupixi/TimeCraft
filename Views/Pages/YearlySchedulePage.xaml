<Page x:Class="TimeCraft.YearlySchedulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:TimeCraft"
      mc:Ignorable="d"
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="YearlySchedule">

    <Page.Resources>
        <!-- Style for rounded buttons -->
        <Style TargetType="Button" x:Key="RoundedButtonStyle">
            <Setter Property="Background" Value="#FFEEEEEE" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="White">
            <TextBlock Text="2024" FontSize="24" FontWeight="Bold" Foreground="Black" Margin="10" />
            <Button Content="&lt;" Style="{StaticResource RoundedButtonStyle}" />
            <Button Content="Сегодня" Style="{StaticResource RoundedButtonStyle}" />
            <Button Content="&gt;" Style="{StaticResource RoundedButtonStyle}" />
            <Button Content="День" Style="{StaticResource RoundedButtonStyle}" Margin="0,0,2,0" />
            <Button Content="Неделя" Style="{StaticResource RoundedButtonStyle}" Margin="2" />
            <Button Content="Месяц" Style="{StaticResource RoundedButtonStyle}" Margin="2" />
            <Button Content="Год" Style="{StaticResource RoundedButtonStyle}" Margin="2,0,0,0" />
        </StackPanel>


        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <UniformGrid Columns="4" Rows="3">
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>
                <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Calendar DisplayMode="Month" />
                </Border>

            </UniformGrid>
        </ScrollViewer>
    </Grid>
</Page>
