<UserControl x:Class="TimeCraft.CalendarTaskControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeCraft"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Панель задач -->
        <Border Grid.Column="0" Background="#FF2D2D30" BorderBrush="#FF2D2D30">
            <StackPanel>
                <!-- Здесь будет список задач -->
                <!-- Пример одной задачи -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="8:00" Foreground="White" FontSize="16" FontWeight="Bold" Margin="5"/>
                    <TextBlock Text="Завтрак" Foreground="White" FontSize="16" Margin="5"/>
                </StackPanel>
                <!--  другие задачи   -->
            </StackPanel>
        </Border>

        <!-- Календарь -->
        <Calendar Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20"/>
    </Grid>
</UserControl>
